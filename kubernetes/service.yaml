apiVersion: v1
kind: Service
metadata:
  name: enterprise-devops-service
spec:
  type: NodePort
  selector:
    app: enterprise-devops-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
